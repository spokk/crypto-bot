{
  "name": "crypto-bot",
  "version": "1.0.0",
  "description": "Crypto bot.",
  "main": "api/bot.js",
  "type": "module",
  "scripts": {
    "deploy": "vercel --prod",
    "eslint": "eslint . --ext .js,.mjs,.cjs --fix",
    "prettier": "prettier --write .",
    "format": "npm run eslint && npm run prettier"
  },
  "dependencies": {
    "dotenv": "^17.2.4",
    "telegraf": "^4.16.3"
  },
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.0",
    "globals": "^17.3.0",
    "prettier": "3.8.1"
  }
}
